{
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "name": "@yourkitchen/common",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "typescript:deploy": "tsc -b",
    "typescript:clean": "rm -f -rf ./dist && rm -f tsconfig.tsbuildinfo"
  },
  "babel": {
    "presets": [
      [
        "@babel/preset-react",
        {
          "runtime": "automatic"
        }
      ]
    ]
  },
  "dependencies": {
    "@yourkitchen/models": "^1.0.0",
    "axios": "^0.21.4",
    "gql-query-builder": "^3.5.5",
    "react": "^17.0.2"
  }
}
